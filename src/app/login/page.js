import LoginPage from "@/app/pages/login";

export const metadata = {
  title: "Login | BladeMaster",
  description: "Sign in to your account.",
};

export default function Page() {
  return <LoginPage />;
}
